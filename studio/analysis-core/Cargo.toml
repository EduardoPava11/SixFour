[package]
name = "analysis-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# gif decode/encode — image-rs, MIT OR Apache-2.0 (vetted).
gif = "0.13"
