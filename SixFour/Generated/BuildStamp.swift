// GENERATED every build by project.yml 'build stamp (git SHA + time, on-screen)'.
// The committed values are a placeholder; the build script overwrites them so the
// running binary shows its exact git SHA + build time (makes stale builds visible).
enum BuildStamp {
    static let gitSHA = "placeholder"
    static let buildTime = "placeholder"
}
